from django.utils.translation import ugettext_lazy as _

TRANSACTION_TYPES_CODES = (
           ('A',_('Adjustment')),
           ('P',_('Payment')),   
           ('S',_('Sale')),
           ('R',_('Refund')),
        )
